services:
  codesniffer:
    image: php:8.5.5-cli-alpine
    volumes:
      - ./:/var/pdga
    working_dir: /var/pdga

  composer:
    command: -V
    image: composer:2.9.7
    volumes:
      - ./:/var/pdga
    working_dir: /var/pdga
